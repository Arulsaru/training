import { RoundImageDirective } from './round-image.directive';

describe('RoundImageDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundImageDirective();
    expect(directive).toBeTruthy();
  });
});
