import { SecondsDirective } from './seconds.directive';

describe('SecondsDirective', () => {
  it('should create an instance', () => {
    const directive = new SecondsDirective();
    expect(directive).toBeTruthy();
  });
});
